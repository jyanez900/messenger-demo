server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: portal-jacobdemo
  cloud:
    config:
      profile: ${CONFIG_PROFILE:local}
      uri: ${CONFIG_SERVICE_URL:http://localhost:8888}
  http:
    converters:
      preferred-json-mapper: gson



